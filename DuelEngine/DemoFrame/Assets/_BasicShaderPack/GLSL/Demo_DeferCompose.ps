#version 330

uniform sampler2D targetTexture;

in 	Varing
{
	vec4	pos;
	vec2	texcoord;
} PS_StageInput;

layout(location = 0)		out	vec4	color0;

void main(void)
{
	color0 = texture(targetTexture, PS_StageInput.texcoord);
}