#version 330

uniform sampler2D targetTexture;

in 	Varing
{
	vec2	texcoord;
} PS_StateInput;

layout(location = 0)		out	vec4	color0;

void main(void)
{
	color0 = texture(targetTexture, PS_StateInput.texcoord);
}